@page
@model Sator.App.Presentacion.VentanaModel
@{
}
<link rel="stylesheet" href="~/css/site.css" />
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
@{
	foreach (var producto in Model.producto_list)
	{
		if (producto.id == Model.idProducto)
		{
			Model.productoI=producto;
		}
	}
}
<div>
	<h1>@Model.productoI.nombre</h1>
</div>
<!--<div class="modal fade" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="staticBackdropLabel">@Model.productoI.nombre</h5>
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
			<p class="card-text">@Model.productoI.tipoproducto.descripcion</p>
			<p class="card-text">@Model.productoI.descripcion</p>
			<h6 class="modal-title" id="staticBackdropLabel">Ingredientes</h6>
			@{
				string ingredientes="";
				foreach (var ingrediente in Model.productoI.ingrediente)
				{
					if (ingredientes=="")
					{
						ingredientes = ingrediente.descripcion;
					}else{
						ingredientes = ingredientes+", "+ ingrediente.descripcion;
					}
				}
			}
			<p class="card-text">@ingredientes</p>
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-primary">Agregar</button>
        </div>
      </div>
    </div>
  </div>-->